<style>
.mh_404 {
  min-height: 80vh;
  display: flex;
  align-items: center;
  justify-content: center;
  background: radial-gradient(ellipse at center, #0f172a 0%, #020617 60%);
  color: #fff;
  text-align: center;
  overflow: hidden;
  font-family: Inter, Geist, Roboto, system-ui, -apple-system, sans-serif;
}

.mh_404 .container {
  position: relative;
  max-width: 720px;
  padding: 80px 20px;
}

.mh_404 .code {
  font-size: clamp(80px, 18vw, 180px);
  font-weight: 900;
  letter-spacing: -6px;
  background: linear-gradient(90deg, #22d3ee, #818cf8, #a855f7);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  animation: float 6s ease-in-out infinite;
}

.mh_404 h1 {
  font-size: 32px;
  margin: 10px 0 12px;
  font-weight: 700;
}

.mh_404 p {
  font-size: 16px;
  color: #cbd5f5;
  max-width: 520px;
  margin: 0 auto 30px;
  line-height: 1.6;
}

.mh_404 .actions {
  display: flex;
  gap: 15px;
  justify-content: center;
  flex-wrap: wrap;
}

.mh_404 .btn {
  min-height: 48px;
  padding: 0 28px;
  border-radius: 30px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  font-size: 14px;
  font-weight: 600;
  letter-spacing: .4px;
  text-decoration: none;
  transition: all .3s ease;
}

.mh_404 .btn.primary {
  background: linear-gradient(135deg, #6366f1, #a855f7);
  color: #fff;
  box-shadow: 0 10px 25px rgba(99,102,241,.35);
}

.mh_404 .btn.primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 15px 35px rgba(99,102,241,.5);
}

.mh_404 .btn.ghost {
  border: 1px solid rgba(255,255,255,.15);
  color: #e5e7eb;
  background: transparent;
}

.mh_404 .btn.ghost:hover {
  background: rgba(255,255,255,.06);
}

.mh_404 .glow {
  position: absolute;
  inset: 0;
  background:
    radial-gradient(circle at 30% 30%, rgba(99,102,241,.15), transparent 45%),
    radial-gradient(circle at 70% 60%, rgba(168,85,247,.15), transparent 45%);
  z-index: -1;
}

@keyframes float {
  0%,100% { transform: translateY(0); }
  50% { transform: translateY(-12px); }
}
</style>

<section class="mh_404">
  <div class="container">
    
    <div class="content">
      <span class="code">404</span>
      <h1>Oops! Page Not Found</h1>
      <p>
        The page you're looking for doesn’t exist or has been moved.
        Let’s get you back on track.
      </p>

      <div class="actions">
        <a href="{{ routes.root_url }}" class="btn primary">Back to Home</a>
        <a href="/collections/all" class="btn ghost">Browse Courses</a>
      </div>
    </div>

    <div class="glow"></div>

  </div>
</section>

